name: Run CLI on Push to Main

on:
  push:
    branches:
      - main

jobs:
  run-cli:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    
    - name: Run CLI
      run: |
        make push
